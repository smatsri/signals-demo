---
description: Commit message conventions for this repo
alwaysApply: true
---

### Commit message format

Use a short Conventional Commits–style line:

```text
<type>(optional-scope): <short, imperative summary>
```

- **Types**: `feat`, `fix`, `chore`, `refactor`, `docs`, `test`, `build`, `ci`.
- **Scope**: optional, kebab-case (e.g. `todos`, `auth-api`).
- **Summary**: imperative, no trailing period. Keep it brief.

No body or footer required.
